interface:
  display_name: "外部视频创意报告"
  short_description: "读取外部完整视频案例并归并可供下游复用的创意结构"
  default_prompt: "使用 $onion-creative-report 生成当前外部视频创意报告。"
dependencies:
  tools:
    - type: "mcp"
      value: "onion-agent"
      description: "通过统一OAuth冻结外部创意案例并回查必需详情"
      transport: "streamable_http"
      url: "https://intel-mcp.guanghexinzhi.cn/agent/mcp"
policy:
  allow_implicit_invocation: true
