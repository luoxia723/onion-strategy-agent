interface:
  display_name: "内部视频创意报告"
  short_description: "读取内部完整视频、富化和表现背景并归并可复用创意结构"
  default_prompt: "使用 $onion-internal-creative-report 生成最近完整周期的内部视频创意报告。"
dependencies:
  tools:
    - type: "mcp"
      value: "onion-agent"
      description: "通过统一OAuth复用内部完整素材池并回查创意事实"
      transport: "streamable_http"
      url: "https://intel-mcp.guanghexinzhi.cn/agent/mcp"
policy:
  allow_implicit_invocation: true
