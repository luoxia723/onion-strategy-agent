# 洋葱策略 Agent｜首次使用

## 你需要准备什么

- 已安装并登录 Codex 桌面端；
- 已获得该私有 GitHub 仓库的访问权限，或拿到管理员发放的 ZIP；
- 一枚尚未使用、尚未过期的一次性激活 Token；
- 在非公司网络环境中，确保团队代理可用。

> 不需要配置火山、腾讯云、数据库、对象存储或其他供应商 API Key。这些服务凭据由统一 MCP 在服务端封装。

不要求使用者预先安装Node.js、FFmpeg、Pillow或云厂商SDK。本地确定性脚本需要Python 3.10+；Git只用于克隆和后续Pull。Python或Git缺失时，Codex可以在取得本机安装确认后调用包内初始化脚本处理。

## 第一次打开

1. 在 Codex 中打开**该仓库的根目录**。不要只打开`.agents/skills/`，也不要打开它的上级批量下载目录。
2. Codex 询问是否信任项目时，选择信任。只有信任后，项目内`.codex/config.toml`才会生效。
3. Codex 会自动扫描`.agents/skills/`并加载本角色的 12 个 Skill，不用逐个安装。
4. 如果项目在 Codex 打开期间刚完成更新，重新打开项目或重启 Codex，让 Skill 和 MCP 配置重新加载。

## 初始化本机环境

在Codex中直接说：

> 初始化项目环境。先只检查；如果需要安装本机软件，说明将安装什么并等我确认后再继续。

Codex按系统运行：

```text
Windows: powershell -ExecutionPolicy Bypass -File scripts/bootstrap.ps1 -Mode Check
macOS:   /bin/bash scripts/bootstrap.sh --check
```

检查发现缺少Python或Git并获得确认后，改为：

```text
Windows: powershell -ExecutionPolicy Bypass -File scripts/bootstrap.ps1 -Mode Install
macOS:   /bin/bash scripts/bootstrap.sh --install
```

初始化会把Python虚拟环境建在`.runtime/venv`，不会写入Git。Windows优先使用系统`winget`，macOS已有Homebrew时使用`brew`；系统没有对应包管理器时脚本停止并给出明确安装入口，不会静默下载执行未知脚本。已有Codex桌面端时，策略任务优先复用应用自带的Codex执行器，不要求为了CLI安装Node.js。

## 连接 onion-agent MCP

1. 在 Codex 打开 **Settings → MCP servers**。
2. 找到`onion-agent`，点击 **Authenticate**。
3. 浏览器会打开激活页。把管理员单独发送的一次性 Token 粘贴到这个页面。
4. 成功后回到 Codex，输入`/mcp`确认`onion-agent`已连接；输入`/skills`确认 Skill 已加载。

Token 只在认证页输入一次，不放在`.env`、`.codex/config.toml`、聊天消息、业务产物或 Git 提交中。OAuth 凭据由 Codex 管理，正常更新项目时不需要重新输入 Token。

## 运行首次环境检查

最简单的方式是直接对 Codex 说：

> 运行首次环境检查，并用通俗的话告诉我哪些通过、哪些需要处理。

也可以在仓库根目录的终端运行：

```text
python scripts/first_run_check.py
```

Windows 如果`python`命令不可用，尝试`py scripts/first_run_check.py`；macOS 也可以用`python3 scripts/first_run_check.py`。该检查只读，会验证 Python、Git、仓库完整性、Skill 数量、项目 MCP 配置和远程服务健康状态；不读取或输出任何 Token。

## 第一次功能验证

认证完成后，先做只读测试：

> 请告诉我当前是什么角色、可用的 Skill 有哪些，并检查 onion-agent MCP 是否已连接。不要生成媒体，不要执行付费调用。

再按岗位需求发起一个真实任务。需要付费、上传、写入或生成媒体的步骤，仍会按各 Skill 的确认门禁执行。

## 常见问题

| 现象 | 先检查什么 |
|---|---|
| 看不到 Skill | 确认打开的是仓库根目录；确认`.agents/skills/`存在；然后重新打开项目或重启 Codex。 |
| 提示没有Python | 对Codex说“初始化项目环境”，确认后让它运行当前系统的Install模式。 |
| Windows不能运行脚本 | 确认使用包内`bootstrap.ps1`命令；如果公司策略禁用PowerShell安装，改为由IT安装Python 3.10+和Git。 |
| macOS没有Homebrew | 脚本不会自动安装Homebrew；由使用者从python.org安装Python，或先按Homebrew官方方式安装后重试。 |
| 没有Node.js或FFmpeg | 正常；正式角色流程不需要安装，不能把它当成环境失败。 |
| 看不到`onion-agent` | 确认项目已信任、`.codex/config.toml`存在，然后重新打开项目。 |
| 激活页 403 或无法打开 | 检查是否在允许的网络或是否已连接团队代理。 |
| Token 无效 | Token 可能已过期或已被使用；向管理员申请新的一次性 Token。 |
| 浏览器认证完成但 Codex 没连上 | 返回 Settings → MCP servers 重试 Authenticate，并检查系统浏览器、本机防火墙和回调页是否被拦截。 |
| MCP 已显示但任务取不到数 | 先运行首次环境检查；如远程服务正常，再把任务、时间范围和报错原文发给管理员。 |
| 无法 Pull 私有仓库 | 检查 GitHub 账号是否已被授权；ZIP试用包不支持自动 Pull。 |

## 后续更新与产物保护

- 使用 GitHub Desktop 点击 Pull，或对 Codex 说“更新项目到最新”。
- 自动更新只允许 fast-forward；如系统维护区有本地修改，会停止并提示，不会强制覆盖。
- 输入、草稿、审核记录和产物都放在`工作区/`；这些目录与`.runtime/`被 Git 忽略，不参与上游更新。
- 不要手工修改`.agents/`、`产品资料/`、`.codex/`或`scripts/`；如需改业务逻辑，由维护者在主仓库修改后发布新版本。

官方参考：[Codex Skills](https://developers.openai.com/codex/skills)、[Codex MCP](https://developers.openai.com/codex/mcp)。
